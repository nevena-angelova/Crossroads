@model DateTime?
@{
    var value = "";
    if (Model.HasValue)
    {
        value = String.Format("{0:d}", Model.Value.ToString("dd/mm/yyyy", System.Globalization.CultureInfo.InvariantCulture));
    }
}
@Html.TextBox("", value, new { @id = "datepicker"})

