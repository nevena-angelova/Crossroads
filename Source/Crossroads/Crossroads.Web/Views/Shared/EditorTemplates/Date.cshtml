@using System.Globalization;
@model DateTime?
@{
    var value = "";
    if (Model.HasValue)
    {
        value = String.Format("{0:d}", Model.Value.ToString("MM/dd/yyyy", new CultureInfo("en-US")));
    }
}
@Html.TextBox("", value, new { @id = "datepicker"})

